# Prometheus alertmanager. Simple build RPM

- [ ] Необходимо поработать с правами на файлы

Сборка rpm-пакета prometheus-alertmanager

```bash
git clone https://github.com/KasperWPS/alertmanager_rpm.git
```

```bash
cd alertmanager_rpm
```

```bash
./build.sh
```